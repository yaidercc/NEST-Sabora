export enum GeneralRoles {
    client = "client",
    employee = "employee",
    admin = "admin"
}

export enum EmployeeRoles {
    client = "client",
    employee = "employee",
    admin = "admin"
}
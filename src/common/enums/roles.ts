export enum GeneralRoles {
    CLIENT = "client",
    EMPLOYEE = "employee",
    ADMIN = "admin"
}

export enum EmployeeRoles {
    COOKER = "cooker",
    CASHIER = "cashier",
    WAITRESS = "waitress",
    MANAGER = "manager"
}
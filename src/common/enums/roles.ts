export enum GeneralRoles {
    client = "client",
    employee = "employee",
    admin = "admin"
}

export enum EmployeeRoles {
    cooker = "cooker",
    cashier = "cashier",
    waitress = "waitress"
}